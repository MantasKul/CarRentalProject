#pragma once

#include <string>

using namespace std;

// Class for adding/deleting table
class createDeleteTable {
public:
	int createTable(const string);
};

// Populating or Updating an existing table
class manipTable {
public:
	int addData(const string);
};