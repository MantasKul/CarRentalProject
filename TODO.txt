?Delete Table function
Hash password creating user

carList columns:
ID, MAKE, MODEL, COLOR, LICENSE_PLATE, AVAILABILITY, NOTES
ID should be auto incremented
AVAILABILITY should be 1 by default

Renting car function:
user msut not have a car rented atm (RENTED_CAR !=-1)

Add unrent function or smth perhaps;

UserList:
Make user name unique if it isn't already

UPDATE carList SET AVAILABILITY=0 WHERE ID=1;UPDATE userList SET RENTED_CAR=0 WHERE NAME=;

UPDATE carList SET AVAILABILITY=1 WHERE ID=(SELECT RENTED_CAR FROM userList WHERE NAME=''); UPDATE userList SET RENTED_CAR=-1 WHERE NAME='';